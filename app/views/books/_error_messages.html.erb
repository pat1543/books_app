<% if @book.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      <%= t('books.error_messages') %>
    </div>
    <ul>
      <% @book.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
