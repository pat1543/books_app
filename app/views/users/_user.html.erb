<tbody>
<tr>
  <td>
    <% if user.avatar.attached? %>
      <%= image_tag user.avatar.variant(resize: '80x80').processed, class: "rounded-circle px-3" %>
    <% end %>
    <%= link_to user.name, user %>
  </td>
</tr>
</tbody>
